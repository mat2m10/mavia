<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Save the Date</title>
<style>
  body {
    font-family: 'Open Sans', Arial, sans-serif;
    background-color: #b3d1ff; /* Light blue background as indicated in the sketch */
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
  }
  .container-fluid {
    width: 80%; /* Adjusted for a larger central container */
    max-width: 800px; /* Maximum width */
    position: relative;
    text-align: center;
    margin: auto; /* Centralize the container */
  }
  .backdrop {
    background: url('<%= asset_url('boat_harlingen.jpg') %>') no-repeat center center;
    background-size: cover;
    padding: 100px; /* Increase padding to make the central picture larger */
    position: relative; /* For absolute positioning of child elements */
    border-radius: 20px; /* Optional, for rounded corners */
  }
  .announcement-text {
    color: #6B2527; /* Adjusted text color */
    font-size: 32px; /* Adjust size as needed */
    font-weight: bold;
  }
  .knot-text {
    font-size: 24px; /* Smaller font size for the subtitle */
    font-weight: normal;
  }
  .white-box {
    background: rgba(255, 255, 255, 0.5); /* 50% transparent white background */
    display: inline-block;
    padding: 40px;
    border-radius: 5px;
    margin-top: 20px; /* Space between text and box */
  }
  .form-signin {
    display: inline-block;
    width: 100%;
  }
  .form-label-group {
    margin-bottom: 10px;
  }
  .btn-primary {
    background-color: #6B2527; /* Red color button as per sketch */
    color: white;
    border: none;
    padding: 10px;
    border-radius: 5px;
    width: 100%;
  }

  .btn-primary {
    /* ... Other styles remain unchanged ... */
    transition: background-color 0.3s; /* Smooth transition for hover effect */
  }

  .btn-primary:hover {
    background-color: #ce7c3f; /* Change button color on hover */
  }

  .footer-info {
    display: flex;
    justify-content: space-between; /* Aligns the text to the sides */
    position: absolute;
    width: 100%;
    bottom: 10px; /* Adjust as needed */
    left: 0;
    padding: 0 20px; /* Same as backdrop padding for alignment */
    font-size: 16px; /* Adjust as needed */
    color: #6B2527;
  }

  .announcement-text, .footer-info span {
    color: #fff; /* Change to white for better visibility */
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.9); /* Text shadow for readability */
  }
  .btn-primary:active {
      background-color: 3e2d2b !important; /* Replace with the green color you want */
  }

  .flash {
  padding: 10px;
  border: 1px solid #ccc;
  margin-bottom: 10px;
  text-align: center;
}

.flash.alert {
  background-color: #f8d7da;
  border-color: #f5c6cb;
  color: #721c24;
}

.flash.notice {
  background-color: #d1ecf1;
  border-color: #bee5eb;
  color: #0c5460;
}
  
  @media screen and (max-width: 768px) {
    .container-fluid {
      width: 100%; /* Make it full-width under 768px */
      margin: 0; /* Remove margins */
    }
    .backdrop {
      padding: 80px; /* Reduce padding for smaller screens */
    }
  }
</style>
</head>
<body>
<% flash.each do |key, value| %>
  <div class="flash <%= key %>"><%= value %></div>
<% end %>
<div class="container-fluid">
  <div class="backdrop">
    <div class="announcement-text">
      Livia & Matthieu<br>
      <span class="knot-text">Are Tying the Knot!</span>
    </div>
    <div class="white-box">
      <h2>Answer their invitation</h2>
      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "form-signin" }) do |f| %>
        <div class="form-label-group">
          <%= f.input :email,
                      required: false,
                      autofocus: true,
                      input_html: { autocomplete: "email", class: "form-control", placeholder: "Email" },
                      label_html: { class: "sr-only" },
                      wrapper_html: { class: "form-group" }
          %>
        </div>

        <div class="form-label-group">
          <%= f.input :password,
                      required: false,
                      input_html: { autocomplete: "current-password", class: "form-control", placeholder: "Password" },
                      label_html: { class: "sr-only" },
                      wrapper_html: { class: "form-group" }
          %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, "Log in", class: "btn btn-lg btn-primary" %>
        </div>
      <% end %>
    </div>
    <div class="footer-info">
      <span>Kasteel Hillenraad</span>
      <span>25 May 2024</span>
    </div>
  </div>
</div>

</body>
</html>
